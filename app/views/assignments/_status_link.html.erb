<% if @assignment.status == "not yet begun" %>
	<li>
		<%= button_to "Begin Task", assignment_path(@assignment, :assignment => {:started_at => Time.now}), :method => 'put', :remote => true %>
	</li>
<% elsif @assignment.status == "on hold" %>
	<li>
		<%= button_to "Resume Task", assignment_path(@assignment, :assignment => 
			{:hold => false}), :method => 'put', :remote => true %>
	</li>
<% elsif @assignment.status == "in progress" %>
	<li>
		<%= button_to "Pause", assignment_path(@assignment, :assignment => { :hold => true, :paused_at => Time.now }), :method => 'put', :remote => true%>

		<%= button_to "Mark as completed", assignment_path(@assignment, :assignment => { :completed_at => Time.now }), :method => 'put', :remote => true %>
	</li>
<% end %>