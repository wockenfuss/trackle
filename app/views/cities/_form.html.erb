<%= form_for @city do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :deadline %>
<%= f.text_field :deadline, :placeholder => "Don't use this yet" %>

<%= f.label :color %>
<%= f.text_field :color, :class => "colorPicker" %>

<%= f.submit %>
<% end %>