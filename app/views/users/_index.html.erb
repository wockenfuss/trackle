<% if @users.any? %>
	<% @users.each do |user| %>
	<ul>
		<li>
			<%= user.name %>
			<%= link_to "delete", user_path(user), :method => :delete, :confirm => "Are you sure you want to clone all of this models?" %>
		</li>
	</ul>
	<% end %>
<% else %>
	<p><%= "No users yet." %></p>
<% end %>