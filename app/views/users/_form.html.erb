<%= form_for @user do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :email %>
<%= f.text_field :email %>
<%= f.label :admin %>
<%= check_box_tag :admin, @user.id, @user.has_role?(:admin) %>

<%= f.submit %>
<%= f.submit :Cancel if !@user.new_record? %>	

<% end %>