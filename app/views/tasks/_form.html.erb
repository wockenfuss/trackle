<div class="resourceColumn">
	<%= form_for @task do |f| %>

		<div>
			<%= f.label :name %>
			<%= f.text_field :name %>
		</div>
		<div>
			<%= f.label :color %>
			<%= f.text_field :color, :class => "colorPicker" %>
		</div>	
			
		<%= f.submit %>
	<% end %>
</div>