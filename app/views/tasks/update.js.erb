$('#taskList').slideToggle('fast', function(e) {
	$(this).html('<%= j render "tasks/form_and_index" %>')
	$('.colorPicker').minicolors();
	$(this).slideToggle();
  $('.formDisplayLink').off('click').on('click', function(e) {
 		$(this).next().slideToggle();
 	});
});