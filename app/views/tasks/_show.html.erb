<div class="taskBox draggable" data-task="<%= task.id %>" data-city="<%= city.id %>" data-taskColor="<%= task.color || "#ccc" %>">
	<%= task.name %>
	<div class="taskProgress">
		<div class="completeTasks">		
			<%= task_progress_bar(task, city) %>
		</div>
		<!-- <div class="incompleteTasks"></div> -->
	</div>
</div>